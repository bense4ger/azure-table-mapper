/**
 * Created by benseager on 09/09/15.
 */
var formatter = require('../lib/formatter');

describe('formatter.azurifyRowKey', function () {
    describe('transforming values to row keys', function () {
        it('should handle a single value', function () {

        });
        it('should handle multiple values', function () {

        });
    });
    describe('errors are thrown if', function () {
        it('no value is passed', function () {

        });
        it('the wrong type of value is passed', function () {

        });
    });
});

describe('formatter.parseRowKey', function () {
    describe('transforming row keys to standard values', function () {
        it('should handle a single value', function () {
            
        });
        it('should handle multiple values', function () {
            
        });
    });
    describe('it can fail', function () {
        
    });
});